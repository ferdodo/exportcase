name: 🔍 Pull Request Check

on:
  pull_request:
    branches: master

permissions:
  id-token: write
  contents: write

jobs:
  test:
    name: 🐑 Test
    uses: ./.github/workflows/test.yml
    with:
      ref_name: ${{ github.head_ref }}