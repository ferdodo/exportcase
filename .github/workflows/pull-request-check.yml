name: 🔍 Pull Request Check

on:
  pull_request:
    branches: master

jobs:
  package:
    name: 📦 Package
    uses: ./.github/workflows/package.yml

  test:
    name: 🐑 Test
    uses: ./.github/workflows/test.yml
    needs: package